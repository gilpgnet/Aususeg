<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" 
  package="net.ramptors.aususeg">
  <uses-permission android:name="android.permission.INTERNET" />
  <application android:label="@string/app_name" 
    android:theme="@style/AppTheme">
    <activity android:name=".CtrlIndex">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".CtrlInicia" 
      android:label="@string/iniciar_sesion"/>
    <activity android:name=".CtrlSesion" 
      android:label="@string/sesion"/>
    <activity android:name=".CtrlInvitados" 
      android:label="@string/solo_para_invitados" />
    <activity android:name=".CtrlClientes" 
      android:label="@string/solo_para_clientes" />
  </application>
</manifest>